#include "headers.h"

vector<double> jordanGauss(vector<vector<double>> matrix) {

    if (empty(matrix)) {
        return false;
    }

    n = matrix.size();         //rows
    m = matrix[0].size();      //cols

}